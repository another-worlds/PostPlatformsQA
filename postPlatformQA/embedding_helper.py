from dotenv import load_dotenv
# Simple app ( text -> embed -> vsearch) + standart st
# Chat app( += history aware retriever )+ chat st
# Chat embed ( += pop-up window) 

# simple app


# Load secrets with dotenv
# Initialize prebuilt document QA agent and pass LLM
# prebuilt doc processing
# context + input prompt + history
#

